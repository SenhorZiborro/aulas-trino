# Mesmo environment do coordinator — senão o nó não entra no cluster.
node.environment=lab
# node.id propositalmente ausente: os dois workers compartilham este arquivo
# e cada um gera o próprio id na inicialização.
node.data-dir=/data/trino
